/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterRebalance : The fields common to a running and a completed rebalance.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterRebalance {
    /// The ID of this rebalance.
    #[serde(rename = "rebalanceId")]
    pub rebalance_id: i64,
    /// Every partition in the rebalance plan and its progress within this rebalance.
    #[serde(rename = "partitions")]
    pub partitions: Vec<models::ClusterRebalanceOperationPartition>,
    /// When this rebalance was created.
    #[serde(rename = "startedAt")]
    pub started_at: chrono::DateTime<chrono::FixedOffset>,
}

impl ClusterRebalance {
    /// The fields common to a running and a completed rebalance.
    pub fn new(
        rebalance_id: i64,
        partitions: Vec<models::ClusterRebalanceOperationPartition>,
        started_at: chrono::DateTime<chrono::FixedOffset>,
    ) -> ClusterRebalance {
        ClusterRebalance {
            rebalance_id,
            partitions,
            started_at,
        }
    }
}
