/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ExportingStatusCode : The exporting status of a physical tenant, aggregated over every replica of every one of its partitions: - `EXPORTING`: all replicas are exporting and committing their position. - `PAUSED`: all replicas are paused, nothing is being exported. - `SOFT_PAUSED`: all replicas keep exporting but do not commit their position. - `MIXED`: replicas report different phases, so the tenant is in no single phase.
/// The exporting status of a physical tenant, aggregated over every replica of every one of its partitions: - `EXPORTING`: all replicas are exporting and committing their position. - `PAUSED`: all replicas are paused, nothing is being exported. - `SOFT_PAUSED`: all replicas keep exporting but do not commit their position. - `MIXED`: replicas report different phases, so the tenant is in no single phase.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum ExportingStatusCode {
    #[serde(rename = "EXPORTING")]
    Exporting,
    #[serde(rename = "PAUSED")]
    Paused,
    #[serde(rename = "SOFT_PAUSED")]
    SoftPaused,
    #[serde(rename = "MIXED")]
    Mixed,
}

impl std::fmt::Display for ExportingStatusCode {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Exporting => write!(f, "EXPORTING"),
            Self::Paused => write!(f, "PAUSED"),
            Self::SoftPaused => write!(f, "SOFT_PAUSED"),
            Self::Mixed => write!(f, "MIXED"),
        }
    }
}

impl Default for ExportingStatusCode {
    fn default() -> ExportingStatusCode {
        Self::Exporting
    }
}
