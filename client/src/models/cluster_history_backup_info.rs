/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterHistoryBackupInfo : A history backup id and what each physical tenant reports for it. No cluster-level state is aggregated from the per-tenant states.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterHistoryBackupInfo {
    /// The id of the backup.
    #[serde(rename = "backupId")]
    pub backup_id: i64,
    /// What each physical tenant reports for this backup id, ordered by physical tenant id. When looking a backup id up directly, every targeted tenant is listed, including the ones reporting `NOT_FOUND`. Within a listing, only the tenants that hold the id are listed.
    #[serde(rename = "physicalTenants")]
    pub physical_tenants: Vec<models::ClusterHistoryBackupTenantInfo>,
}

impl ClusterHistoryBackupInfo {
    /// A history backup id and what each physical tenant reports for it. No cluster-level state is aggregated from the per-tenant states.
    pub fn new(
        backup_id: i64,
        physical_tenants: Vec<models::ClusterHistoryBackupTenantInfo>,
    ) -> ClusterHistoryBackupInfo {
        ClusterHistoryBackupInfo {
            backup_id,
            physical_tenants,
        }
    }
}
