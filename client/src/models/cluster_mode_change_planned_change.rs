/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterModeChangePlannedChange : The operations of a cluster mode change that apply to one physical tenant.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterModeChangePlannedChange {
    /// The physical tenant the operations apply to; null for operations that are not scoped to a single physical tenant, such as broker lifecycle operations.
    #[serde(rename = "physicalTenantId", deserialize_with = "Option::deserialize")]
    pub physical_tenant_id: Option<String>,
    /// The ordered list of operations that will be applied to the physical tenant.
    #[serde(rename = "operations")]
    pub operations: Vec<models::ClusterModeChangeOperation>,
}

impl ClusterModeChangePlannedChange {
    /// The operations of a cluster mode change that apply to one physical tenant.
    pub fn new(
        physical_tenant_id: Option<String>,
        operations: Vec<models::ClusterModeChangeOperation>,
    ) -> ClusterModeChangePlannedChange {
        ClusterModeChangePlannedChange {
            physical_tenant_id,
            operations,
        }
    }
}
