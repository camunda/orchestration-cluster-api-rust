/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AgentInstanceLimits : The configured limits for an agent instance, set once at creation.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct AgentInstanceLimits {
    /// Maximum LLM calls allowed. -1 if no limit is configured.
    #[serde(rename = "maxModelCalls")]
    pub max_model_calls: i32,
    /// Maximum tool calls allowed. -1 if no limit is configured.
    #[serde(rename = "maxToolCalls")]
    pub max_tool_calls: i32,
    /// Maximum total tokens allowed. -1 if no limit is configured.
    #[serde(rename = "maxTokens")]
    pub max_tokens: i64,
}

impl AgentInstanceLimits {
    /// The configured limits for an agent instance, set once at creation.
    pub fn new(max_model_calls: i32, max_tool_calls: i32, max_tokens: i64) -> AgentInstanceLimits {
        AgentInstanceLimits {
            max_model_calls,
            max_tool_calls,
            max_tokens,
        }
    }
}
