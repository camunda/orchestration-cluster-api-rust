/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterRuntimeBackupTakeResult : Whether one physical tenant's runtime backup was triggered, and under which id it can be monitored and deleted.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterRuntimeBackupTakeResult {
    /// The id of the physical tenant.
    #[serde(rename = "physicalTenantId")]
    pub physical_tenant_id: String,
    /// The id to monitor or delete this physical tenant's backup by: the id it is running under when `TRIGGERED` — the requested one, or the one the tenant generated when ids are generated — and the requested id to check when `UNKNOWN`. Null when the tenant is known to be running no backup, and also when an `UNKNOWN` tenant generates its own ids, because the id it may be running under was never reported; list that tenant's backups to find it.
    #[serde(rename = "backupId", deserialize_with = "Option::deserialize")]
    pub backup_id: Option<i64>,
    /// What this physical tenant did with the trigger.
    #[serde(rename = "outcome")]
    pub outcome: models::ClusterRuntimeBackupTakeOutcome,
    /// Why this physical tenant reported no triggered backup. Null when it was triggered.
    #[serde(rename = "reason", deserialize_with = "Option::deserialize")]
    pub reason: Option<String>,
}

impl ClusterRuntimeBackupTakeResult {
    /// Whether one physical tenant's runtime backup was triggered, and under which id it can be monitored and deleted.
    pub fn new(
        physical_tenant_id: String,
        backup_id: Option<i64>,
        outcome: models::ClusterRuntimeBackupTakeOutcome,
        reason: Option<String>,
    ) -> ClusterRuntimeBackupTakeResult {
        ClusterRuntimeBackupTakeResult {
            physical_tenant_id,
            backup_id,
            outcome,
            reason,
        }
    }
}
