/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterTopologyResponse : The topology of the whole cluster, aggregated over all physical tenants.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterTopologyResponse {
    /// The brokers that are part of this cluster, across all physical tenants.
    #[serde(rename = "brokers")]
    pub brokers: Vec<models::ClusterBrokerInfo>,
    /// The cluster Id.
    #[serde(rename = "clusterId", deserialize_with = "Option::deserialize")]
    pub cluster_id: Option<String>,
    /// The number of brokers in the cluster.
    #[serde(rename = "clusterSize")]
    pub cluster_size: i32,
    /// The version of the Orchestration Cluster Gateway.
    #[serde(rename = "gatewayVersion", deserialize_with = "Option::deserialize")]
    pub gateway_version: Option<String>,
    /// The topology of each physical tenant of this cluster.
    #[serde(rename = "physicalTenants")]
    pub physical_tenants: Vec<models::PhysicalTenantTopology>,
}

impl ClusterTopologyResponse {
    /// The topology of the whole cluster, aggregated over all physical tenants.
    pub fn new(
        brokers: Vec<models::ClusterBrokerInfo>,
        cluster_id: Option<String>,
        cluster_size: i32,
        gateway_version: Option<String>,
        physical_tenants: Vec<models::PhysicalTenantTopology>,
    ) -> ClusterTopologyResponse {
        ClusterTopologyResponse {
            brokers,
            cluster_id,
            cluster_size,
            gateway_version,
            physical_tenants,
        }
    }
}
