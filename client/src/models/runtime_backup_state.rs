/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RuntimeBackupState : Information about the checkpoint and backup state of the physical tenant.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RuntimeBackupState {
    /// List of partition checkpoint states.
    #[serde(rename = "checkpointStates")]
    pub checkpoint_states: Vec<models::PartitionCheckpointState>,
    /// List of partition backup states.
    #[serde(rename = "backupStates")]
    pub backup_states: Vec<models::PartitionBackupState>,
    /// List of partition backup ranges.
    #[serde(rename = "ranges")]
    pub ranges: Vec<models::PartitionBackupRange>,
}

impl RuntimeBackupState {
    /// Information about the checkpoint and backup state of the physical tenant.
    pub fn new(
        checkpoint_states: Vec<models::PartitionCheckpointState>,
        backup_states: Vec<models::PartitionBackupState>,
        ranges: Vec<models::PartitionBackupRange>,
    ) -> RuntimeBackupState {
        RuntimeBackupState {
            checkpoint_states,
            backup_states,
            ranges,
        }
    }
}
