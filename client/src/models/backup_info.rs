/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// BackupInfo : Detailed status of a runtime backup. The aggregated state is computed from the backup state of each partition as: - If the backup of all partitions is 'COMPLETED', the overall state is 'COMPLETED'. - If one partition is 'FAILED', the overall state is 'FAILED'. - Otherwise, if one partition is 'DOES_NOT_EXIST', the overall state is 'INCOMPLETE'. - Otherwise, if one partition is 'IN_PROGRESS', the overall state is 'IN_PROGRESS'.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BackupInfo {
    /// The id of the backup.
    #[serde(rename = "backupId")]
    pub backup_id: i64,
    /// The aggregated state of the backup.
    #[serde(rename = "state")]
    pub state: models::StateCode,
    /// Reason for failure if the state is 'FAILED'.
    #[serde(rename = "failureReason", deserialize_with = "Option::deserialize")]
    pub failure_reason: Option<String>,
    /// Detailed status of the backup per partition. Always contains every partition of the physical tenant.
    #[serde(rename = "details")]
    pub details: Vec<models::PartitionBackupInfo>,
}

impl BackupInfo {
    /// Detailed status of a runtime backup. The aggregated state is computed from the backup state of each partition as: - If the backup of all partitions is 'COMPLETED', the overall state is 'COMPLETED'. - If one partition is 'FAILED', the overall state is 'FAILED'. - Otherwise, if one partition is 'DOES_NOT_EXIST', the overall state is 'INCOMPLETE'. - Otherwise, if one partition is 'IN_PROGRESS', the overall state is 'IN_PROGRESS'.
    pub fn new(
        backup_id: i64,
        state: models::StateCode,
        failure_reason: Option<String>,
        details: Vec<models::PartitionBackupInfo>,
    ) -> BackupInfo {
        BackupInfo {
            backup_id,
            state,
            failure_reason,
            details,
        }
    }
}
