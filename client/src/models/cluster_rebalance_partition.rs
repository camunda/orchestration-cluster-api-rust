/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterRebalancePartition : One partition's leadership/balance status - its current leader, its desired leader, and whether a rebalance is currently moving it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterRebalancePartition {
    /// The unique ID of this partition, within its physical tenant.
    #[serde(rename = "partitionId")]
    pub partition_id: i32,
    /// The partition group this partition belongs to. Partition IDs are unique only within a group, so this is needed to identify the partition.
    #[serde(rename = "physicalTenantId")]
    pub physical_tenant_id: String,
    /// The broker ID currently leading this partition, or absent if it has no leader.
    #[serde(rename = "currentLeader", deserialize_with = "Option::deserialize")]
    pub current_leader: Option<String>,
    /// The broker ID the current configuration wants to lead this partition.
    #[serde(rename = "desiredLeader")]
    pub desired_leader: String,
    /// Whether this partition is being actively transferred, unbalanced, or balanced.
    #[serde(rename = "state")]
    pub state: State,
}

impl ClusterRebalancePartition {
    /// One partition's leadership/balance status - its current leader, its desired leader, and whether a rebalance is currently moving it.
    pub fn new(
        partition_id: i32,
        physical_tenant_id: String,
        current_leader: Option<String>,
        desired_leader: String,
        state: State,
    ) -> ClusterRebalancePartition {
        ClusterRebalancePartition {
            partition_id,
            physical_tenant_id,
            current_leader,
            desired_leader,
            state,
        }
    }
}
/// Whether this partition is being actively transferred, unbalanced, or balanced.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum State {
    #[serde(rename = "TRANSFERRING")]
    Transferring,
    #[serde(rename = "UNBALANCED")]
    Unbalanced,
    #[serde(rename = "BALANCED")]
    Balanced,
}

impl Default for State {
    fn default() -> State {
        Self::Transferring
    }
}
