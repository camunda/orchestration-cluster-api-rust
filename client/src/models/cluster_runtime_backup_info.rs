/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ClusterRuntimeBackupInfo : A runtime backup id, what each physical tenant reports for it, and the state aggregated over every targeted tenant — folded from the per-tenant states by the same rules a per-tenant state is folded from its partitions.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ClusterRuntimeBackupInfo {
    /// The id of the backup.
    #[serde(rename = "backupId")]
    pub backup_id: i64,
    /// The state aggregated over every targeted physical tenant, whether the backup id was looked up directly or listed. A tenant holding nothing for this id counts as `DOES_NOT_EXIST`, so the aggregate is `INCOMPLETE` unless every targeted tenant holds the backup.
    #[serde(rename = "state")]
    pub state: models::StateCode,
    /// Reason for failure if the aggregated state is 'FAILED'.
    #[serde(rename = "failureReason", deserialize_with = "Option::deserialize")]
    pub failure_reason: Option<String>,
    /// What each physical tenant reports for this backup id, ordered by physical tenant id. Every targeted tenant is listed, including the ones reporting `DOES_NOT_EXIST`.
    #[serde(rename = "physicalTenants")]
    pub physical_tenants: Vec<models::ClusterRuntimeBackupTenantInfo>,
}

impl ClusterRuntimeBackupInfo {
    /// A runtime backup id, what each physical tenant reports for it, and the state aggregated over every targeted tenant — folded from the per-tenant states by the same rules a per-tenant state is folded from its partitions.
    pub fn new(
        backup_id: i64,
        state: models::StateCode,
        failure_reason: Option<String>,
        physical_tenants: Vec<models::ClusterRuntimeBackupTenantInfo>,
    ) -> ClusterRuntimeBackupInfo {
        ClusterRuntimeBackupInfo {
            backup_id,
            state,
            failure_reason,
            physical_tenants,
        }
    }
}
