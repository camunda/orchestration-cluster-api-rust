/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AgentInstanceHistoryCommitStatusEnum : The commit status of a history item. COMMITTED: the producing job completed successfully. PENDING: the producing job is still active (in-flight). DISCARDED: the producing job failed; this item was superseded by a later activation.
/// The commit status of a history item. COMMITTED: the producing job completed successfully. PENDING: the producing job is still active (in-flight). DISCARDED: the producing job failed; this item was superseded by a later activation.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum AgentInstanceHistoryCommitStatusEnum {
    #[serde(rename = "COMMITTED")]
    Committed,
    #[serde(rename = "PENDING")]
    Pending,
    #[serde(rename = "DISCARDED")]
    Discarded,
}

impl std::fmt::Display for AgentInstanceHistoryCommitStatusEnum {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Committed => write!(f, "COMMITTED"),
            Self::Pending => write!(f, "PENDING"),
            Self::Discarded => write!(f, "DISCARDED"),
        }
    }
}

impl Default for AgentInstanceHistoryCommitStatusEnum {
    fn default() -> AgentInstanceHistoryCommitStatusEnum {
        Self::Committed
    }
}
