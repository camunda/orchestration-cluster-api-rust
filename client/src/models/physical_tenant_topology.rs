/*
 * Orchestration Cluster API
 *
 * API for communicating with a Camunda 8 cluster.  ## Conventions  ### Enum value casing  Server-side, enum-typed request fields accept any casing of a documented value (for example `ACTIVE`, `active`, and `Active` are all accepted on input). Responses always use the canonical casing shown in this specification.  This permissive request behavior is a server-side convenience and is **not** part of the OpenAPI enum contract — SDKs generated from this specification by strict generators may reject non-canonical casing on the client side. For portability across SDKs, always send the canonical casing shown in this document.
 *
 * The version of the OpenAPI document: 0.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PhysicalTenantTopology : The topology of a single physical tenant.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PhysicalTenantTopology {
    /// The id of the physical tenant.
    #[serde(rename = "physicalTenantId")]
    pub physical_tenant_id: String,
    /// The number of partitions spread across this physical tenant.
    #[serde(rename = "partitionsCount")]
    pub partitions_count: i32,
    /// The configured replication factor for this physical tenant.
    #[serde(rename = "replicationFactor")]
    pub replication_factor: i32,
    /// ID of the last completed change of this physical tenant.
    #[serde(rename = "lastCompletedChangeId")]
    pub last_completed_change_id: String,
    /// The brokers holding partitions of this physical tenant.
    #[serde(rename = "brokers")]
    pub brokers: Vec<models::PhysicalTenantBrokerTopology>,
}

impl PhysicalTenantTopology {
    /// The topology of a single physical tenant.
    pub fn new(
        physical_tenant_id: String,
        partitions_count: i32,
        replication_factor: i32,
        last_completed_change_id: String,
        brokers: Vec<models::PhysicalTenantBrokerTopology>,
    ) -> PhysicalTenantTopology {
        PhysicalTenantTopology {
            physical_tenant_id,
            partitions_count,
            replication_factor,
            last_completed_change_id,
            brokers,
        }
    }
}
