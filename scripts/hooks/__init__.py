"""Numbered post-processing hooks for the generated Camunda client crate.

See ``scripts/postprocess.py`` for the orchestrator that builds a shared
:class:`~scripts.hooks.common.Context` and runs the hooks in ``NUMBER`` order.
"""
